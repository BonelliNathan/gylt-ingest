# Vault settings — minimal pour GYLT Ingest

```yaml
vault_path: "{VAULT_PATH}"
agent_folder: "99 - Cursor"
scripts_dir: "10 - Scripts"
knowledge_dir: "03 - Knowledge"
daily_notes_dir: "00 - Daily notes"
langue: fr
user_name: USER
```

Adapter `user_name` et chemins selon le vault.
